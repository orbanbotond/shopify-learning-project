h2 Products in #{@group.name}
 
ul
  -@group.shopify_products.each do |product|
   li= link_to product.title, "https://#{@shop_session.url}/admin/products/#{product.shopify_product_id}", target: "_top"
